# TODO

## Immediate (Correctness)
- [ ] Verify substation J-signs in **DcElectricSolver**: current source a→b => `-I` at `a`, `+I` at `b`.
- [ ] Verify **Substation.stamp** uses the same injection signs and includes diode/backfeed and `maxCurrentA` clamp.
- [ ] Ensure `P_substations_out = I * (Va - Vb)` (positive when delivering to network).
- [ ] Confirm `GridResult` fields used by `ResultCsvWriter` exist: per-node V, per-device I/P, requested power, brake power.
- [ ] Re-check **Balance/Mismatch** in writer: use only network-facing terms (exclude device-internal losses).

## Tests
- [ ] **A0 Open circuit** (provided): no lines, no trains. Expect `V(bus)≈E`, `I[S]≈0`, `P[S]≈0`.
- [ ] **A1 Single line** between bus and ground return (finite R). Expect `I[S]=E/R`, `P_lines=I^2*R`, `V(bus)=E - I*R_line_to_ground` if modeled.
- [ ] **A2 Two substations** in parallel (no trains) with identical E,R. Expect each shares current equally (if symmetric).
- [ ] **A3 One train motoring**: positive `P_train`, `I[S]>0`, `P_lines>0`.
- [ ] **A4 One train braking with diode blocked**: `P_brake≈|P_req|`, `P_trains≈aux`, `UnderReceptivity>0`.
- [ ] **A5 One train braking regenerative**: negative `P_train` (returning), `I[S]` may drop; balance holds.

## Instrumentation
- [ ] Add assertion: if solver finds `Va - Vb > E + tol` for a diode-locked station while it is stamped ON, throw or log error.
- [ ] Optional runtime logging of `(Va,Vb,E,R,I_src,rawI,dV)` for first few steps for one station.

## Performance / Flow
- [ ] Keep `Root` FAST mode with backpressure ACK to avoid outpacing I/O.
- [ ] Consider buffered CSV writes with periodic flush, but keep ack after flush in FAST mode.

## Docs
- [ ] Merge tech spec and keep as `docs/DC_GRID_TechSpec_v1.md` with sign conventions and equations.
- [ ] Document test configs (A0..A5) and expected results.